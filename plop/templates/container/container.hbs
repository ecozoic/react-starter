import { connect, MapStateToProps, MapDispatchToProps } from 'react-redux';
import { Dispatch } from 'redux';

import { State } from '../../reducers';
import {{component}} from '../../components/{{component}}';

export interface {{name}}StateProps {}

export interface {{name}}DispatchProps {}

const mapStateToProps: MapStateToProps<{{name}}StateProps, {}> =
  (state: State) => ({});

const mapDispatchToProps: MapDispatchToProps<{{name}}DispatchProps, {}> =
  (dispatch: Dispatch<State>) => ({});

const {{name}}Container = connect<{{name}}StateProps, {{name}}DispatchProps, {}>(
  mapStateToProps,
  mapDispatchToProps,
)({{component}});

export default {{name}}Container;
